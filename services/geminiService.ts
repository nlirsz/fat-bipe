// Arquivo mantido vazio ou com funções de suporte não-IA se necessário.
export const generateMatchSummary = async (): Promise<string> => {
  return "Resumo de IA desativado.";
};

export const suggestTeams = async (): Promise<null> => {
  return null;
};

export const parseScoutImage = async (): Promise<null> => {
  return null;
};
